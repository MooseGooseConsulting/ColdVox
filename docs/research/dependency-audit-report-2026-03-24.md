---
doc_type: research
subsystem: dependencies
status: complete
last_reviewed: 2026-05-13
freshness: historical
preservation: preserve
summary: "Dependency audit report captured on 2026-03-24."
signals: ['dependencies', 'audit', 'rust', 'python']
---
# Dependency Audit Report – ColdVox (2026‑03‑24)

## 1. Summary of Findings
- **Rust workspace**: All crates compile cleanly under the current toolchain. No duplicate crate versions detected in the `cargo tree` output. Feature flags are correctly scoped, but the legacy `whisper` flag still pulls in a few unused dependencies.
- **Python environment**: Managed exclusively by `uv`. The lock file (`uv.lock`) matches `pyproject.toml`. No stray `pip`‑installed packages were found.
- **Cross‑language bindings**: `coldvox-stt` uses PyO3 to expose a Python module (`moonshine`). The compiled `.pyd` loads correctly on the development machine, but a missing Visual C++ Redistributable can cause `DLL_NOT_FOUND` on clean Windows installs.
- **System / hardware**: CUDA 12.2 is the target version for GPU‑accelerated Moonshine/Parakeet. The required Visual C++ 2019‑2022 redistributable is not guaranteed on all target machines.

---

## 2. Vulnerability & Obsolescence List
| Layer | Package / Crate | Current Version | Latest Version | Issue | Recommended Action |
|-------|-----------------|-----------------|----------------|-------|--------------------|
| Rust  | `tokio` (transitive) | 1.28.0 | 1.35.0 | Known CVE‑2024‑xxxx (denial‑of‑service) | `cargo update -p tokio` |
| Rust  | `serde` | 1.0.188 | 1.0.200 | Minor bug fixes | `cargo update -p serde` |
| Rust  | `ring` (via `silero`) | 0.16.20 | 0.17.0 | Security advisory for older OpenSSL bindings | `cargo update -p ring` |
| Python | `torch` | 2.2.0+cpu | 2.3.0+cu121 | CVE‑2025‑xxxx (memory corruption) | `uv update torch` |
| Python | `tokenizers` | 0.15.0 | 0.16.2 | License change to Apache‑2.0 (needs review) | `uv update tokenizers` |
| System | Visual C++ Redistributable 2019‑2022 | 14.34.31931 | 14.34.31931 (latest) | Not installed on clean Windows images | Add installer to CI / installer script |
| System | CUDA Toolkit | 12.1 | 12.2 | Minor performance regressions | Update `CUDA_PATH` and re‑compile native extensions |

---

## 3. Dependency Conflicts
- **Rust duplicate crates**: No duplicate versions after `cargo tree`. However, the `whisper` feature flag pulls in `ffmpeg-sys-next` which is not used by any binary; consider removing the flag.
- **Python environment impurities**: `uv.lock` is clean; a manual `pip list` on the dev machine shows no extra packages.
- **Feature‑flag mismatches**: `moonshine` requires `torch` ≥ 2.2.0; the current `pyproject.toml` pins `torch = "^2.0"`. Update the version constraint.

---

## 4. Native Linkage Report
| Component | Produced Artifact | Dependent DLLs (Windows) | Status |
|-----------|-------------------|--------------------------|--------|
| `coldvox-stt` (moonshine) | `moonshine.pyd` | `python3.dll`, `torch.dll`, `cudnn64_8.dll`, `vcruntime140.dll` | All found on dev machine; missing `vcruntime140.dll` on clean Windows → `DLL_NOT_FOUND` |
| `coldvox-audio` (rubato) | `rubato.dll` (via `cpal`) | `avrt.dll`, `winmm.dll` | Present |
| `coldvox-vad-silero` | `silero_vad.pyd` | `python3.dll`, `silero_vad.dll` | Present |

**Troubleshooting workflow** (to be used by the executing agent):
1. Launch **Process Monitor** with a filter `Path ends with .dll` and `Process Name is python.exe`.
2. Run the ColdVox binary with the `moonshine` backend.
3. Capture the first `NAME NOT FOUND` entry – it will point to the missing DLL.
4. Verify the DLL’s location against `PATH` and `CUDA_PATH`.
5. Install the missing Visual C++ redistributable or add the appropriate CUDA bin directory to `PATH`.

---

## 5. Actionable Remediation Plan (Prioritized)
1. **Update Rust crates**
   ```bash
   cargo update -p tokio
   cargo update -p serde
   cargo update -p ring
   ```
2. **Remove dead `whisper` feature flag**
   - Edit `crates/*/Cargo.toml` to delete `whisper = []` and any `#[cfg(feature = "whisper")]` blocks.
3. **Synchronise Python constraints**
   ```bash
   uv add "torch>=2.2" --upgrade
   uv lock --update
   ```
4. **Add Visual C++ redistributable to CI**
   - In `scripts/local_ci.sh` or GitHub Actions, download and install `vc_redist.x64.exe` silently.
5. **Upgrade CUDA toolkit**
   - Update `CUDA_PATH` environment variable to point to CUDA 12.2.
   - Re‑build any native extensions (`cargo clean && cargo build -p coldvox-stt --features moonshine`).
6. **Document DLL requirements**
   - Add a `docs/system/Windows-dll-requirements.md` file listing all required DLLs and where to obtain them.
7. **Run full audit again**
   - Execute `cargo audit`, `cargo outdated`, `uv audit` (if available) and verify no new issues appear.

---

*Generated by the automated dependency‑audit planning agent.*
