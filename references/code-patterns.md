# Programming AI Slop Patterns

## High-Confidence Signals
- Variable names: `data`, `result`, `temp`, `item`
- Obvious comments: `# Create a user` before `user = User()`
- Function names: `handleData()`, `processItems()`
